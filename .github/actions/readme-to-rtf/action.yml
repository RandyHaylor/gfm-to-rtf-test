name: 'README to RTF/DOCX/PDF'
description: 'Convert GitHub-flavored Markdown to RTF, DOCX, and PDF based on config'

inputs:
  config:
    description: 'Path to readme-to-rtf-pdf-doc.json config file'
    required: false
    default: 'readme-to-rtf-pdf-doc.json'

runs:
  using: 'composite'
  steps:
    - name: Install Python dependencies
      shell: bash
      run: pip install pygments pillow

    - name: Generate RTF files
      shell: python
      env:
        CONFIG_PATH: ${{ inputs.config }}
        ACTION_PATH: ${{ github.action_path }}
      run: |
        import json
        import os
        import subprocess
        import sys

        config_path = os.environ['CONFIG_PATH']
        action_path = os.environ['ACTION_PATH']

        converter_script = os.path.join(action_path, 'gfm_markdown_to_rtf.py')
        embedder_script = os.path.join(action_path, 'rtf_image_embedder.py')

        with open(config_path) as f:
            config = json.load(f)

        for entry in config.get('files', []):
            input_file = entry['input']
            base_name = input_file.rsplit('.', 1)[0]
            rtf_path = base_name + '.rtf'
            print(f'Converting {input_file} -> {rtf_path}')
            subprocess.run([sys.executable, converter_script, input_file, rtf_path], check=True)
            subprocess.run([sys.executable, embedder_script, rtf_path], check=True)

    - name: Build dotnet converter
      shell: bash
      run: dotnet build ${{ github.action_path }}/rtf-converter/rtf-converter.csproj -c Release -o ${{ github.action_path }}/rtf-converter/bin

    - name: Generate DOCX and PDF files
      shell: python
      env:
        CONFIG_PATH: ${{ inputs.config }}
        ACTION_PATH: ${{ github.action_path }}
      run: |
        import json
        import os
        import subprocess

        config_path = os.environ['CONFIG_PATH']
        action_path = os.environ['ACTION_PATH']
        converter_exe = os.path.join(action_path, 'rtf-converter', 'bin', 'rtf-converter')

        with open(config_path) as f:
            config = json.load(f)

        for entry in config.get('files', []):
            input_file = entry['input']
            formats = entry.get('output_formats', ['rtf'])
            base_name = input_file.rsplit('.', 1)[0]
            rtf_path = base_name + '.rtf'

            extra_outputs = []
            if 'docx' in formats:
                extra_outputs.append(base_name + '.docx')
            if 'pdf' in formats:
                extra_outputs.append(base_name + '.pdf')

            if extra_outputs:
                cmd = [converter_exe, rtf_path] + extra_outputs
                print(f'Converting: {" ".join(cmd)}')
                subprocess.run(cmd, check=True)
